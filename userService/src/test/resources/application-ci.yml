spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/testdb
    username: test
    password: test
    driver-class-name: org.postgresql.Driver

  data:
    redis:
      host: localhost
      port: 6379

testcontainers:
  enabled: false